import { Injectable, inject } from '@angular/core'; import { Observable, shareReplay } from 'rxjs'; import { ApiClientService } from '../http/api-client.service';
export interface PracticaMusicalConceptual { id:number; nombre:string; slug:string; orden:number; definicionBreve:string|null; definicionAmpliada:string|null; descripcionConceptual:string|null; caracteristicas:string|null; contextos:string|null; ejemplos:string|null; fuentes:string|null; recursoVisualUrl:string|null; textoAlternativoRecurso:string|null; }
@Injectable({providedIn:'root'}) export class PracticasMusicalesService { private readonly api=inject(ApiClientService); private readonly catalogo$=this.api.get<PracticaMusicalConceptual[]>('/api/v1/publico/practicas-musicales',{errorFallback:'No fue posible consultar las Prácticas musicales.'}).pipe(shareReplay({bufferSize:1,refCount:true})); listar():Observable<PracticaMusicalConceptual[]> { return this.catalogo$; } consultar(slug:string):Observable<PracticaMusicalConceptual> { return this.api.get(`/api/v1/publico/practicas-musicales/${encodeURIComponent(slug)}`,{errorFallback:'No fue posible consultar esta Práctica musical.'}); } }
