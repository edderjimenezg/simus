import { Component, ElementRef, HostListener, Input, OnChanges, inject, signal } from '@angular/core';
import { RouterLink } from '@angular/router';
import { TerritoriosSonorosService } from '../../../../core/services/territorios-sonoros.service';
import { PracticasMusicalesService } from '../../../../core/services/practicas-musicales.service';

@Component({selector:'app-ayuda-conceptual-catalogo',standalone:true,imports:[RouterLink],template:`<span class="relative inline-block"><button type="button" (click)="alternar($event)" [attr.aria-expanded]="abierto()" [attr.aria-label]="'Información sobre ' + nombre" class="inline-flex h-4 w-4 items-center justify-center rounded-full border border-[#087B3E]/40 bg-white p-0 text-dato font-bold leading-none text-[#087B3E] transition hover:border-[#087B3E] hover:bg-[#00DA5E]/10 focus-visible:outline-none focus-visible:ring-2 focus-visible:ring-[#00DA5E]">i</button>@if (abierto() && concepto(); as dato) { <div role="dialog" [attr.aria-label]="'Información sobre ' + nombre" class="absolute left-0 z-20 mt-2 w-72 rounded-xl border border-slate-200 bg-white p-4 text-left text-xs leading-relaxed text-slate-600 shadow-lg"><p>{{ dato.definicionBreve || 'La ficha conceptual de esta categoría está en preparación.' }}</p><a [routerLink]="ruta()" (click)="cerrar()" class="mt-3 inline-block font-bold text-[#087B3E] no-underline hover:underline">Ver información completa</a></div> }</span>`})
export class AyudaConceptualCatalogoComponent implements OnChanges { @Input({required:true}) tipo!: 'territorio'|'practica'; @Input({required:true}) nombre!:string; private readonly host=inject(ElementRef<HTMLElement>); private readonly territorios=inject(TerritoriosSonorosService); private readonly practicas=inject(PracticasMusicalesService); readonly concepto=signal<{slug:string;definicionBreve:string|null}|null>(null); readonly abierto=signal(false); ngOnChanges():void { const consulta=this.tipo==='territorio'?this.territorios.listar():this.practicas.listar(); consulta.subscribe({next:(items:any[])=>this.concepto.set(items.find(x=>x.nombre===this.nombre)??null)}); } alternar(event:MouseEvent):void { event.stopPropagation(); this.abierto.update(v=>!v); } cerrar():void { this.abierto.set(false); } @HostListener('document:keydown.escape') escapar():void { this.cerrar(); } @HostListener('document:click',['$event']) clicExterno(event:MouseEvent):void { if (!this.host.nativeElement.contains(event.target as Node)) this.cerrar(); } ruta():string[]{const dato=this.concepto();return ['/ecosistema',this.tipo==='territorio'?'territorios-sonoros':'practicas-musicales',dato?.slug??''];} }
